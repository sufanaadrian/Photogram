{
  "version": 2,
  "builds": [
    {
      "src": "*.js",
      "use": "@vercel/node",
      "config": {
        "command": "npm install --force && npm run build"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/" }]
}
